"""Path to folder with sequences.
Should only be changed if sequences are manually copied to a different path.
Default is '/sd/sequence_library'.
"""
library_path = '/flash/sequence_library'

"""Timing accuracy can be 'us' or 'ms'. 'us' mode only works for sequences
with a total duration of less than ~19min47s
"""
accuracy = 'us'
